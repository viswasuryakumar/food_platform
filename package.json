{
  "name": "campus-food-platform",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "setup": "node scripts/setup.js",
    "setup:env": "node scripts/setup.js --env-only",
    "db:start": "node scripts/db.js start",
    "db:stop": "node scripts/db.js stop",
    "db:status": "node scripts/db.js status",
    "seed": "node scripts/seed.js",
    "seed:reset": "node scripts/seed.js --reset",
    "dev": "node scripts/dev.js",
    "setup:run": "npm run setup && npm run db:start && npm run dev",
    "setup:demo": "npm run setup && npm run db:start && npm run seed && npm run dev",
    "dev:with-db": "npm run db:start && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.13.5",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.1.6",
    "uuid": "^13.0.0"
  }
}
